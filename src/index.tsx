import * as React from 'react';
import * as ReactDOM from 'react-dom';
import App from './App';
import './index.scss';

// eslint-disable-next-line no-undef
ReactDOM.render(<App />, document.querySelector('blink-web-ui'));
